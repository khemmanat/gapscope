name: gapscope_workspace
description: GapScope Studio - Flutter component workbench and runtime QA inspector
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dev_dependencies:
  flutter_lints: ^6.0.0
  melos: ^3.0.0
